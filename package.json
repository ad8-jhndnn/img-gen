{
  "name": "img-gen",
  "version": "1.0.0",
  "description": "Simple webserver that returns a random generated PNG image on each request",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "jimp": "^0.22.8"
  }
}
